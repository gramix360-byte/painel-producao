(()=>{
const nativeFetch=window.fetch.bind(window);
async function headers(){const s=await window.PPAuth.getSession();if(!s?.access_token)throw Error('Sessão expirada');return{apikey:window.PPAuth.key,Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json'}}
async function patchOrder(id,data){const h=await headers(),r=await nativeFetch(`${window.PPAuth.url}/rest/v1/orders?id=eq.${encodeURIComponent(id)}`,{method:'PATCH',headers:{...h,Prefer:'return=minimal'},body:JSON.stringify({...data,updated_at:new Date().toISOString()})});if(!r.ok)throw Error(await r.text())}
async function syncFinalized(id){try{await patchOrder(id,{phase:'finalizado',status:'finalizado',finished_at:new Date().toISOString()});window.PPInventory?.adjust?.(id,true);window.KodaAlerts?.refresh?.();window.KodaCommand?.refresh?.();window.PPDashboardPro?.refresh?.();window.KodaDRE?.refresh?.()}catch(e){console.warn('Automação finalização',e)}}
window.fetch=async function(input,init={}){const url=typeof input==='string'?input:input.url,res=await nativeFetch(input,init);try{if(res.ok&&url.includes('/rest/v1/orders')&&init?.method==='PATCH'){const m=url.match(/[?&]id=eq\.([^&]+)/),body=JSON.parse(init.body||'{}'),id=m?decodeURIComponent(m[1]):null;if(id&&(body.status==='finalizado'||body.phase==='finalizado'))setTimeout(()=>syncFinalized(id),120);if(id&&(body.status==='em_producao'||body.phase==='em_producao')){setTimeout(()=>{window.KodaAlerts?.refresh?.();window.PPDashboardPro?.refresh?.()},120)}}if(res.ok&&url.includes('/rest/v1/rpc/convert_quote_to_order')&&init?.method==='POST'){setTimeout(()=>{window.KodaAlerts?.refresh?.();window.KodaCommand?.refresh?.();window.PPDashboardPro?.refresh?.();window.KodaToday?.refresh?.()},250)}}catch(e){console.warn('Workflow automático',e)}return res};
document.addEventListener('click',e=>{const b=e.target.closest('.quote-status-btn[data-status="aprovado"]');if(!b)return;setTimeout(()=>{const card=b.closest('.quote-card'),convert=card?.querySelector('.quote-convert');if(convert&&!convert.disabled){convert.classList.add('workflow-ready');convert.title='Orçamento aprovado: pronto para gerar pedido'}} ,300)});
window.KodaWorkflow={finalize:syncFinalized};
})();