(()=>{const LEGACY_PUR='koda-purchases-v1',LEGACY_SUP='koda-suppliers-v1';let products=[],suppliers=[],purchases=[],filter='todos',search='';const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),money=v=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'}),date=v=>v?new Date((String(v).length===10?v+'T12:00:00':v)).toLocaleDateString('pt-BR'):'—';async function headers(){const s=await window.PPAuth.getSession();return{apikey:window.PPAuth.key,Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json'}}async function loadProducts(){const r=await fetch(`${window.PPAuth.url}/rest/v1/products?select=id,name,sku,stock,cost_price&deleted_at=is.null&active=eq.true&order=name.asc`,{headers:await headers()});if(!r.ok)throw new Error(await r.text());products=await r.json()}async function loadData(){const h=await headers(),[s,p]=await Promise.all([fetch(`${window.PPAuth.url}/rest/v1/suppliers?select=*&active=eq.true&order=name.asc`,{headers:h}),fetch(`${window.PPAuth.url}/rest/v1/purchases?select=*&order=created_at.desc`,{headers:h})]);if(!s.ok)throw new Error(await s.text());if(!p.ok)throw new Error(await p.text());suppliers=await s.json();purchases=await p.json();await migrateLegacy();render()}async function migrateLegacy(){let ls=[],lp=[];try{ls=JSON.parse(localStorage.getItem(LEGACY_SUP)||'[]');lp=JSON.parse(localStorage.getItem(LEGACY_PUR)||'[]')}catch{}if(!ls.length&&!lp.length)return;const h=await headers();if(!suppliers.length&&ls.length){const r=await fetch(`${window.PPAuth.url}/rest/v1/suppliers`,{method:'POST',headers:{...h,Prefer:'return=representation'},body:JSON.stringify(ls.map(x=>({name:x.name,contact:x.contact||null,active:true})))});if(r.ok)suppliers=await r.json()}const byName=n=>suppliers.find(s=>String(s.name).trim().toLowerCase()===String(n||'').trim().toLowerCase());if(!purchases.length&&lp.length){const validUuid=v=>/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(String(v||''));const body=lp.filter(x=>x.product_id).map(x=>{const s=byName(x.supplier);return{supplier_id:s?.id||null,supplier_name:x.supplier||'Fornecedor',product_id:x.product_id,product_name:x.product||'Produto',sku:x.sku||null,quantity:Number(x.qty||0),unit_cost:Number(x.unit||0),total_amount:Number(x.total||0),due_date:x.due_date||null,payment_method:x.payment_method||null,finance_id:validUuid(x.finance_id)?x.finance_id:null,status:x.status==='recebido'?'recebido':'aguardando',received_at:x.receivedAt||null,created_at:x.createdAt||new Date().toISOString()}});if(body.length){const r=await fetch(`${window.PPAuth.url}/rest/v1/purchases`,{method:'POST',headers:{...h,Prefer:'return=representation'},body:JSON.stringify(body)});if(r.ok)purchases=await r.json()}}localStorage.removeItem(LEGACY_SUP);localStorage.removeItem(LEGACY_PUR)}async function createPayable(x){const h=await headers(),p={type:'saida',category:'fornecedor',description:`Compra · ${x.product_name}`,amount:Number(x.total_amount||0),due_date:x.due_date||null,payment_method:x.payment_method||null,supplier_name:x.supplier_name||null,notes:`Pedido de compra KODA${x.sku?` · SKU ${x.sku}`:''}`,status:'pendente',deleted_at:null};const r=await fetch(`${window.PPAuth.url}/rest/v1/cash_transactions`,{method:'POST',headers:{...h,Prefer:'return=representation'},body:JSON.stringify(p)});if(!r.ok)throw new Error(await r.text());return(await r.json())?.[0]?.id||null}async function updatePayable(x){if(!x.finance_id)return;const h=await headers(),body={description:`Compra · ${x.product_name}`,amount:Number(x.total_amount||0),due_date:x.due_date||null,payment_method:x.payment_method||null,supplier_name:x.supplier_name||null,updated_at:new Date().toISOString()};await fetch(`${window.PPAuth.url}/rest/v1/cash_transactions?id=eq.${encodeURIComponent(x.finance_id)}`,{method:'PATCH',headers:{...h,Prefer:'return=minimal'},body:JSON.stringify(body)})}async function addSupplier(){const name=prompt('Nome do fornecedor:');if(!name)return;const contact=prompt('Contato (telefone, e-mail ou WhatsApp):')||'';const r=await fetch(`${window.PPAuth.url}/rest/v1/suppliers`,{method:'POST',headers:{...(await headers()),Prefer:'return=minimal'},body:JSON.stringify({name:name.trim(),contact:contact.trim()||null,active:true})});if(!r.ok)return alert('Não foi possível cadastrar o fornecedor.');await loadData()}async function editSupplier(id){const x=suppliers.find(s=>s.id===id);if(!x)return;const name=prompt('Nome do fornecedor:',x.name);if(!name)return;const contact=prompt('Contato:',x.contact||'');const r=await fetch(`${window.PPAuth.url}/rest/v1/suppliers?id=eq.${encodeURIComponent(id)}`,{method:'PATCH',headers:{...(await headers()),Prefer:'return=minimal'},body:JSON.stringify({name:name.trim(),contact:(contact||'').trim()||null,updated_at:new Date().toISOString()})});if(!r.ok)return alert('Não foi possível editar o fornecedor.');await loadData()}async function deleteSupplier(id){const x=suppliers.find(s=>s.id===id);if(!x||!confirm(`Desativar o fornecedor "${x.name}"?`))return;const r=await fetch(`${window.PPAuth.url}/rest/v1/suppliers?id=eq.${encodeURIComponent(id)}`,{method:'PATCH',headers:{...(await headers()),Prefer:'return=minimal'},body:JSON.stringify({active:false,updated_at:new Date().toISOString()})});if(!r.ok)return alert('Não foi possível desativar o fornecedor.');await loadData()}async function addPurchase(){try{if(!products.length)await loadProducts()}catch{return alert('Não foi possível carregar os produtos.')}if(!suppliers.length)return alert('Cadastre um fornecedor primeiro.');const supplierName=prompt(`Fornecedor:\n${suppliers.map(s=>s.name).join('\n')}`);if(!supplierName)return;const supplier=suppliers.find(s=>s.name.toLowerCase()===supplierName.trim().toLowerCase())||suppliers.find(s=>s.name.toLowerCase().includes(supplierName.trim().toLowerCase()));if(!supplier)return alert('Fornecedor não encontrado.');const term=prompt('Digite o nome ou SKU do produto cadastrado:');if(!term)return;const q=term.toLowerCase().trim(),p=products.find(v=>String(v.sku||'').toLowerCase()===q)||products.find(v=>String(v.name||'').toLowerCase().includes(q));if(!p)return alert('Produto não encontrado.');const quantity=Number(prompt(`Quantidade de ${p.name}:`)||0);if(quantity<=0)return;const unit_cost=Number(String(prompt(`Custo unitário (R$) — atual ${Number(p.cost_price||0).toLocaleString('pt-BR',{minimumFractionDigits:2})}:`)||'0').replace(',','.'));if(unit_cost<0)return;const due=prompt('Vencimento da conta (AAAA-MM-DD). Deixe vazio se não houver:')||'',method=prompt('Forma de pagamento (PIX, Boleto, Cartão, Transferência etc.):')||'';const x={supplier_id:supplier.id,supplier_name:supplier.name,product_id:p.id,product_name:p.name,sku:p.sku||null,quantity,unit_cost,total_amount:quantity*unit_cost,due_date:/^\d{4}-\d{2}-\d{2}$/.test(due)?due:null,payment_method:method||null,status:'aguardando'};try{x.finance_id=await createPayable(x);const r=await fetch(`${window.PPAuth.url}/rest/v1/purchases`,{method:'POST',headers:{...(await headers()),Prefer:'return=minimal'},body:JSON.stringify(x)});if(!r.ok)throw new Error(await r.text());window.PPFinance?.refresh?.();await loadData();alert('Pedido de compra criado e conta a pagar gerada no Financeiro.')}catch(e){console.error(e);alert('Não foi possível criar a compra.')}}async function editPurchase(id){const x=purchases.find(p=>p.id===id);if(!x)return;if(x.status==='recebido')return alert('Compras já recebidas ficam bloqueadas para edição para preservar o estoque e o histórico.');if(x.status==='cancelado')return alert('Esta compra foi cancelada.');const qty=Number(prompt('Quantidade:',x.quantity));if(!qty||qty<=0)return;const cost=Number(String(prompt('Custo unitário:',Number(x.unit_cost||0).toFixed(2).replace('.',','))||'').replace(',','.'));if(cost<0||Number.isNaN(cost))return;const due=prompt('Vencimento (AAAA-MM-DD):',x.due_date||'')||'',method=prompt('Forma de pagamento:',x.payment_method||'')||'';const body={quantity:qty,unit_cost:cost,total_amount:qty*cost,due_date:/^\d{4}-\d{2}-\d{2}$/.test(due)?due:null,payment_method:method||null,updated_at:new Date().toISOString()};const r=await fetch(`${window.PPAuth.url}/rest/v1/purchases?id=eq.${encodeURIComponent(id)}`,{method:'PATCH',headers:{...(await headers()),Prefer:'return=minimal'},body:JSON.stringify(body)});if(!r.ok)return alert('Não foi possível editar a compra.');await updatePayable({...x,...body});window.PPFinance?.refresh?.();await loadData()}async function cancelPurchase(id){const x=purchases.find(p=>p.id===id);if(!x||x.status!=='aguardando')return;if(!confirm(`Cancelar a compra de ${x.product_name}?`))return;const h=await headers();let r=await fetch(`${window.PPAuth.url}/rest/v1/purchases?id=eq.${encodeURIComponent(id)}`,{method:'PATCH',headers:{...h,Prefer:'return=minimal'},body:JSON.stringify({status:'cancelado',updated_at:new Date().toISOString()})});if(!r.ok)return alert('Não foi possível cancelar a compra.');if(x.finance_id)await fetch(`${window.PPAuth.url}/rest/v1/cash_transactions?id=eq.${encodeURIComponent(x.finance_id)}`,{method:'PATCH',headers:{...h,Prefer:'return=minimal'},body:JSON.stringify({deleted_at:new Date().toISOString(),updated_at:new Date().toISOString()})});window.PPFinance?.refresh?.();window.KodaAlerts?.refresh?.();await loadData()}async function receivePurchase(x){if(x.status==='recebido')return;const h=await headers(),pr=await fetch(`${window.PPAuth.url}/rest/v1/products?id=eq.${encodeURIComponent(x.product_id)}&select=id,name,sku,stock,cost_price`,{headers:h});if(!pr.ok)throw new Error(await pr.text());const p=(await pr.json())[0];if(!p)throw new Error('Produto não encontrado.');const before=Number(p.stock||0),qty=Number(x.quantity||0),after=before+qty,newCost=Number(x.unit_cost||p.cost_price||0);let r=await fetch(`${window.PPAuth.url}/rest/v1/products?id=eq.${encodeURIComponent(p.id)}`,{method:'PATCH',headers:{...h,Prefer:'return=minimal'},body:JSON.stringify({stock:after,cost_price:newCost})});if(!r.ok)throw new Error(await r.text());r=await fetch(`${window.PPAuth.url}/rest/v1/purchases?id=eq.${encodeURIComponent(x.id)}`,{method:'PATCH',headers:{...h,Prefer:'return=minimal'},body:JSON.stringify({status:'recebido',received_at:new Date().toISOString(),updated_at:new Date().toISOString()})});if(!r.ok)throw new Error(await r.text());window.KodaInventoryHistory?.add?.({product_id:p.id,product_name:p.name,sku:p.sku||'',type:'entrada',quantity:qty,before,after,reason:`Recebimento de compra · ${x.supplier_name}`,source:'compra',purchase_id:x.id});window.PPProducts?.refresh?.();window.KodaSmartInventory?.refresh?.();window.PPFinance?.refresh?.();window.KodaAlerts?.refresh?.();await loadData()}function state(x){if(x.status==='recebido')return'recebido';if(x.status==='cancelado')return'cancelado';if(x.due_date&&new Date(x.due_date+'T23:59:59')<new Date())return'atrasado';return'aguardando'}function visible(x){const s=state(x);if(filter!=='todos'&&s!==filter)return false;const q=search.toLowerCase().trim();return!q||`${x.product_name} ${x.supplier_name} ${x.sku||''}`.toLowerCase().includes(q)}function render(){const host=document.getElementById('view-compras');if(!host)return;const open=purchases.filter(x=>x.status==='aguardando'),received=purchases.filter(x=>x.status==='recebido'),late=open.filter(x=>state(x)==='atrasado'),total=open.reduce((s,x)=>s+Number(x.total_amount||0),0),rows=purchases.filter(visible);host.innerHTML=`<div class="purchase-head"><div><h2>Compras e Fornecedores</h2><p>Compras, recebimentos e financeiro sincronizados no KODA.</p></div><div><button class="button secondary" id="new-supplier">+ Fornecedor</button><button class="button primary" id="new-purchase">+ Pedido de compra</button></div></div><div class="purchase-kpis"><div><small>Fornecedores ativos</small><strong>${suppliers.length}</strong></div><div><small>Compras abertas</small><strong>${open.length}</strong></div><div><small>Compras atrasadas</small><strong>${late.length}</strong></div><div><small>Mercadoria a receber</small><strong>${money(total)}</strong></div></div><div class="purchase-toolbar"><input id="purchase-search" placeholder="Buscar produto, fornecedor ou SKU" value="${esc(search)}"><select id="purchase-filter"><option value="todos" ${filter==='todos'?'selected':''}>Todas</option><option value="aguardando" ${filter==='aguardando'?'selected':''}>Aguardando</option><option value="atrasado" ${filter==='atrasado'?'selected':''}>Atrasadas</option><option value="recebido" ${filter==='recebido'?'selected':''}>Recebidas</option><option value="cancelado" ${filter==='cancelado'?'selected':''}>Canceladas</option></select></div><div class="purchase-grid"><section><div class="purchase-section-title"><h3>Pedidos de compra</h3><span>${rows.length}</span></div>${rows.length?rows.map(x=>{const st=state(x);return`<article class="purchase-card"><div><b>${esc(x.product_name)}</b><small>${esc(x.supplier_name)} · ${Number(x.quantity)} un.${x.sku?` · SKU ${esc(x.sku)}`:''}</small><small>Criado ${date(x.created_at)}${x.received_at?` · recebido ${date(x.received_at)}`:''}</small></div><div><strong>${money(x.total_amount)}</strong><span class="${st}">${st==='recebido'?'Recebido':st==='cancelado'?'Cancelado':st==='atrasado'?'Atrasado':'Aguardando'}</span><small>${x.due_date?`Vence ${date(x.due_date)}`:'Sem vencimento'} · ${x.finance_id?'Conta gerada':'Sem financeiro'}</small></div><div class="purchase-actions">${x.status==='aguardando'?`<button data-receive="${x.id}" class="button secondary">Receber</button><button data-edit-purchase="${x.id}" class="button secondary">Editar</button><button data-cancel-purchase="${x.id}" class="button danger">Cancelar</button>`:''}</div></article>`}).join(''):'<div class="purchase-empty">Nenhuma compra encontrada com este filtro.</div>'}</section><section><div class="purchase-section-title"><h3>Fornecedores</h3><span>${suppliers.length}</span></div>${suppliers.length?suppliers.map(x=>`<article class="supplier-card"><div><b>${esc(x.name)}</b><small>${esc(x.contact||'Sem contato')}</small></div><div class="supplier-actions"><button data-edit-supplier="${x.id}" class="button secondary">Editar</button><button data-delete-supplier="${x.id}" class="button danger">Desativar</button></div></article>`).join(''):'<div class="purchase-empty">Cadastre seu primeiro fornecedor.</div>'}<div class="purchase-history"><h3>Histórico de recebimentos</h3>${received.length?received.slice(0,8).map(x=>`<div><span>${date(x.received_at)}</span><b>${esc(x.product_name)}</b><small>${Number(x.quantity)} un. · ${esc(x.supplier_name)}</small></div>`).join(''):'<div class="purchase-empty">Nenhum recebimento registrado.</div>'}</div></section></div>`;host.querySelector('#new-supplier').onclick=addSupplier;host.querySelector('#new-purchase').onclick=addPurchase;host.querySelector('#purchase-search').oninput=e=>{search=e.target.value;render()};host.querySelector('#purchase-filter').onchange=e=>{filter=e.target.value;render()};host.querySelectorAll('[data-edit-supplier]').forEach(b=>b.onclick=()=>editSupplier(b.dataset.editSupplier));host.querySelectorAll('[data-delete-supplier]').forEach(b=>b.onclick=()=>deleteSupplier(b.dataset.deleteSupplier));host.querySelectorAll('[data-edit-purchase]').forEach(b=>b.onclick=()=>editPurchase(b.dataset.editPurchase));host.querySelectorAll('[data-cancel-purchase]').forEach(b=>b.onclick=()=>cancelPurchase(b.dataset.cancelPurchase));host.querySelectorAll('[data-receive]').forEach(b=>b.onclick=async()=>{const x=purchases.find(v=>v.id===b.dataset.receive);if(!x)return;b.disabled=true;b.textContent='Recebendo...';try{await receivePurchase(x);alert(`Compra recebida. ${Number(x.quantity)} unidade(s) adicionadas ao estoque.`)}catch(e){console.error(e);alert(e.message||'Não foi possível receber a compra.');b.disabled=false;b.textContent='Receber'}})}async function open(){document.querySelectorAll('.view').forEach(v=>v.classList.remove('active'));document.getElementById('view-compras')?.classList.add('active');document.querySelectorAll('#nav button').forEach(b=>b.classList.remove('active'));document.getElementById('purchases-nav')?.classList.add('active');document.getElementById('page-title').textContent='Compras';document.getElementById('page-subtitle').textContent='Fornecedores, pedidos de compra e contas a pagar';try{await Promise.all([loadProducts(),loadData()])}catch(e){console.error(e);alert('Não foi possível carregar Compras.')}}document.addEventListener('DOMContentLoaded',()=>document.getElementById('purchases-nav')?.addEventListener('click',open));window.KodaPurchases={open,render,receivePurchase,refresh:loadData}})();