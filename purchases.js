(()=>{const KEY='koda-purchases-v1',SUP='koda-suppliers-v1';const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const read=k=>{try{return JSON.parse(localStorage.getItem(k)||'[]')}catch{return[]}};const save=(k,v)=>localStorage.setItem(k,JSON.stringify(v));const uid=()=>crypto.randomUUID?crypto.randomUUID():Date.now()+'';function open(){document.querySelectorAll('.view').forEach(v=>v.classList.remove('active'));const host=document.getElementById('view-compras');host.classList.add('active');document.querySelectorAll('.nav-item,.customer-nav-btn,.product-nav-btn,.quotes-nav-btn,.finance-nav-btn,.reports-nav-btn,.marketing-nav-btn,.integrations-nav-btn,.settings-nav-btn,.purchases-nav-btn').forEach(b=>b.classList.remove('active'));document.getElementById('purchases-nav')?.classList.add('active');document.getElementById('page-title').textContent='Compras';document.getElementById('page-subtitle').textContent='Fornecedores, pedidos de compra e recebimentos';render()}function render(){const host=document.getElementById('view-compras'),sup=read(SUP),pur=read(KEY);const pending=pur.filter(x=>x.status!=='recebido'),total=pending.reduce((s,x)=>s+Number(x.total||0),0);host.innerHTML=`<div class="purchase-head"><div><h2>Compras e Fornecedores</h2><p>Organize reposições e acompanhe o que ainda precisa chegar.</p></div><div><button class="button secondary" id="new-supplier">+ Fornecedor</button><button class="button primary" id="new-purchase">+ Pedido de compra</button></div></div><div class="purchase-kpis"><div><small>Fornecedores</small><strong>${sup.length}</strong></div><div><small>Compras abertas</small><strong>${pending.length}</strong></div><div><small>A receber</small><strong>R$ ${total.toLocaleString('pt-BR',{minimumFractionDigits:2})}</strong></div></div><div class="purchase-grid"><section><h3>Pedidos de compra</h3>${pur.length?pur.map(x=>`<article class="purchase-card"><div><b>${esc(x.product)}</b><small>${esc(x.supplier)} · ${x.qty} un.</small></div><div><strong>R$ ${Number(x.total||0).toLocaleString('pt-BR',{minimumFractionDigits:2})}</strong><span class="${x.status==='recebido'?'received':'pending'}">${x.status==='recebido'?'Recebido':'Aguardando'}</span></div>${x.status!=='recebido'?`<button data-receive="${x.id}" class="button secondary">Receber</button>`:''}</article>`).join(''):'<div class="purchase-empty">Nenhum pedido de compra ainda.</div>'}</section><section><h3>Fornecedores</h3>${sup.length?sup.map(x=>`<article class="supplier-card"><b>${esc(x.name)}</b><small>${esc(x.contact||'Sem contato')}</small></article>`).join(''):'<div class="purchase-empty">Cadastre seu primeiro fornecedor.</div>'}</section></div>`;host.querySelector('#new-supplier').onclick=()=>{const name=prompt('Nome do fornecedor:');if(!name)return;const contact=prompt('Contato (telefone, e-mail ou WhatsApp):')||'';const a=read(SUP);a.push({id:uid(),name,contact});save(SUP,a);render()};host.querySelector('#new-purchase').onclick=()=>{const supplier=prompt('Fornecedor:');if(!supplier)return;const product=prompt('Produto comprado:');if(!product)return;const qty=Number(prompt('Quantidade:')||0);if(qty<=0)return;const unit=Number(String(prompt('Custo unitário (R$):')||'0').replace(',','.'));const a=read(KEY);a.unshift({id:uid(),supplier,product,qty,unit,total:qty*unit,status:'aguardando',createdAt:new Date().toISOString()});save(KEY,a);render()};host.querySelectorAll('[data-receive]').forEach(b=>b.onclick=()=>{const a=read(KEY),x=a.find(v=>v.id===b.dataset.receive);if(x){x.status='recebido';x.receivedAt=new Date().toISOString();save(KEY,a);render();alert('Compra marcada como recebida. Na próxima etapa vamos ligar este recebimento diretamente à entrada do estoque.')}})}document.addEventListener('DOMContentLoaded',()=>document.getElementById('purchases-nav')?.addEventListener('click',open));window.KodaPurchases={open,render}})();